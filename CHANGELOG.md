# 更新记录

本项目使用语义化版本号记录面向用户的变化。

## 2.0.0-alpha.3 — PostgreSQL 一致性读模型

- 新增 PostgreSQL 会话鉴权：access token 仅以 SHA-256 哈希查询，返回 principal 不暴露 token/设备哈希原值。
- 新增按服务端成员关系限定的家庭列表、完整 bootstrap 与增量 pull 读模型；库存、流水、清单、做菜记录均强制携带 `household_id`，食谱进度和偏好额外按当前 `user_id` 隔离。
- bootstrap 和 pull 在同一连接的 `REPEATABLE READ READ ONLY` 事务快照执行，防止跨表状态与 cursor 来自不同数据库时刻。
- pull 支持 minimum cursor、`FULL_RESYNC_REQUIRED`、`limit + 1` 分页和单调 next cursor。
- 新增 4 项 PostgreSQL 读模型测试；2.0 专项增至 48 项，全量增至 67 项。
- 本机没有 Docker、`psql` 或 PostgreSQL 服务，因此本版本明确只证明 SQL 映射和事务边界；真实 migration/并发/回滚集成验证仍未完成。

## 2.0.0-alpha.2 — 数据权利与接口防护

- 新增可替换 `RateLimiter`，对微信登录、邀请、同步 push、迁移、数据导出和注销按不同额度限流；返回标准 429、`Retry-After`，限流键只保留哈希。
- 新增脱敏个人数据导出：包含当前有权读取的家庭共享事实与本人进度/偏好，排除微信身份 subject、token、设备指纹、邀请密钥和其他成员个人偏好。
- 新增账号注销冷静期状态机：owner 必须先转移或删除家庭；申请后撤销其他会话，受限会话只允许查看/取消；执行时删除身份映射和个人状态，共享审计事实以“已注销成员”保留。
- 新增 `data_export_jobs`、`account_deletion_requests` PostgreSQL migration、唯一待注销约束和到期扫描索引。
- 新增“数据与账号”小程序页及 Remote Gateway/Service 接口；正式云开关关闭时只显示本地模式说明，不发起请求。
- 自动测试增至 63 项，其中 2.0 专项 44 项；14 个页面和 136 个小程序文件通过静态检查，新增页面完成微信开发者工具渲染回归。
- 该 Alpha 仍不连接生产数据库；分布式 Redis 限流、生产任务 worker、对象存储加密/过期清理和真实删除恢复演练仍是上线门禁。

## 2.0.0-alpha.1 — 家庭协作与事务边界

- 保持游客模式和 1.2 本地数据路径不变，云同步开关继续关闭。
- 新增同步冲突中心：冲突按操作解释，可重新加入队列或取消本机操作；成员权限变化后的旧命令禁止重试。
- 新增“家庭空间”和“家庭成员”页面，支持创建/切换家庭、邀请加入、成员角色调整、移除与所有权转移；所有云操作在未配置时保持禁用。
- API 路由增加严格运行时 schema、统一 400/413 错误；服务接口改为可异步注入，便于接入真实数据库。
- 新增 PostgreSQL `SERIALIZABLE` 事务执行器：同一事务完成成员权限复核、幂等重放、家庭 cursor 分配、变更日志和 `processed_mutations`；做菜批次使用 `FOR UPDATE` 后复用纯 TypeScript FEFO 规则。
- 自动测试增至 57 项，其中 2.0 专项 38 项；加入 cursor 为 0 的首次快照、冲突恢复、家庭登录/切换、HTTP schema、请求体上限和 PostgreSQL 事务边界回归。
- 此版本仍是 Alpha 工程检查点；完整 PostgreSQL 服务、真实双设备联调与生产合规尚未完成。

## 2.0.0-alpha.0 — 家庭同步基础架构

- 保持游客模式默认本地运行，不自动登录、不自动联网，也不改变 1.2 用户数据。
- 新增内部用户、微信身份、设备会话、家庭空间、四级角色权限和邀请流程。
- 新增 mutation 幂等、增量 cursor、tombstone、冲突箱、离线 Outbox、指数退避与完整重同步协议。
- 新增服务端 FEFO 事务模型、做菜流水、个人食谱进度与偏好隔离、v1 显式双确认迁移。
- 新增 Fastify API、OpenAPI 契约、PostgreSQL 初版 schema、威胁模型与数据库选型记录。
- 新增“家庭与云同步”预览页；生产 API 与 PostgreSQL 接入完成前，登录入口保持安全关闭。
- 自动测试增至 46 项，其中 2.0 专项 27 项。
- 此版本是可审查、可继续联调的 Alpha 工程检查点，不是可承载真实用户数据的生产版本。

## 1.2.0 — 本地体验收口

- 新增 JSON 导入：从剪贴板读取、校验数据结构与引用、确认后导入。
- 导入前自动保留完整本地备份，支持一键恢复且恢复操作也可再次回退。
- 购入页升级为“再次购入”，记住上次数量与保存方式，并提供常用数量快捷项。
- 食谱库新增菜名/食材/标签搜索、“库存齐全”和“我的收藏”筛选。
- 食谱从 10 道扩充至 16 道，并保持三种解锁规则与库存匹配能力。
- 自动测试扩充至 19 个场景，覆盖导入回退、食谱搜索收藏和快捷购入。
- 优化应用图标体积，并增强微信开发者工具灰度基础库下的组件路径兼容性。
- 已于 2026-08-13 上传微信开发版本，并覆盖原体验版。

## 1.1.0 — 冰箱有数

- 产品品牌由内部代号“食仓”升级为“冰箱有数”。
- 新增深绿冰箱应用图标，统一鼠尾草绿、奶油白和暖橙视觉语言。
- 重写首页、“我的”、分享卡片和主要用户文案。
- 新增品牌规范与本地数据存储说明。
- 保留 `pantry:v1:*` Storage key，已有 1.0 本地数据无需迁移即可继续使用。
- 完成 TypeScript、15 个自动测试、静态检查、发布门禁和开发者工具渲染复验。
- 已于 2026-08-12 上传微信开发版本，并自动覆盖原体验版。

## 1.0.0 — MVP

- 实现食材购入、批次库存、新鲜度、食谱解锁和 availability。
- 实现 FEFO 跨批次做菜扣减与 `CookingRecord`。
- 实现食谱缺料、购物清单和转购入闭环。
- 实现本地优先 Repository 架构并预留 `CloudRepository`。
- 完成微信开发者工具上传。
