# 提审审核说明（可复制到微信公众平台）

小程序名称：冰箱有数

版本功能：个人食材批次库存管理、新鲜度进度提醒、食谱解锁、库存匹配、按预计到期日优先（FEFO）的做菜扣库存，以及购物清单转购入。

本版本无需登录，不连接后端，不调用网络接口。用户录入的食材批次、食谱进度、做菜记录和购物清单仅保存在当前微信客户端本地 Storage；用户可在“我的”中导出 JSON 或清空全部数据。

建议审核路径：

1. 首页点击“购入食材”，选择鸡蛋，购入 3 枚，冷藏。
2. 再购入 3 枚鸡蛋，返回“冰箱”，可看到鸡蛋聚合数量，详情页显示两个独立批次。
3. 进入食谱库 → 蒸鸡蛋；该 starter 食谱显示“可解锁”，点击“解锁并加入已掌握”。
4. 若缺盐，可从配料区将缺料加入购物清单；购入所需盐后返回。
5. 点击“开始做菜”，确认页会显示将要扣减的库存批次；只有点击“完成烹饪并扣库存”后才真正修改库存并生成记录。
6. 在购物清单中点击未勾选项，可选择“转入仓库”并进入预填好的购入页。

新鲜度文案仅表示用户自定义/默认建议保存期的时间进度，不是食品安全判定；超过期限统一显示“超过建议期”，不会宣称食材已经变质。
