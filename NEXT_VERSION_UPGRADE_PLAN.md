# 冰箱有数下一版升级计划

版本建议：`1.3.0`「今日行动版」  
计划状态：待开发  
制定日期：2026-08-13  
稳定基线：`1.2.0` 纯本地版  
并行技术线：`2.0.0-alpha.10`，继续开发但不在本版打开云同步

## 1. 版本结论

下一版不以“再增加多少功能”为目标，而要把现有库存闭环变成用户每天愿意执行的行动闭环：

> 打开 10 秒知道先吃什么，3 步开始做菜，30 秒把冰箱重新盘准。

外部仍使用产品名“冰箱有数”；“今日行动版”只作为版本主题，不增加新的品牌名称。

这一版优先解决三个问题：

1. **决策成本高**：用户知道冰箱里有菜，但不知道现在最值得做哪一道。
2. **维护成本高**：每次使用、丢弃或实际数量变化后，如果记录麻烦，库存很快失真。
3. **完成感弱**：做完菜只是扣掉库存，没有让用户感到“我处理了临期食材、今天这顿有成果”。

本版继续坚持原始产品定位：核心不是浏览菜谱，而是围绕用户的真实库存回答“有什么、先吃什么、能做什么、做完还剩什么”。

## 2. 为什么是 1.3，而不是直接开放 2.0

当前稳定版已具备批次、新鲜度、库存匹配、食谱解锁、FEFO 做菜和购物清单闭环。2.0 Alpha 已有身份、家庭、同步、PostgreSQL 和任务 worker，但真实数据库、双设备、合规与预发验证尚未完成。

如果下一版把主要精力放在登录和家庭共享，能提升技术完整度，却不能直接证明用户会更愿意记录食材、打开应用或真正做完一道菜。因此：

- `1.3.0` 先证明个人核心场景的使用价值与交互效率。
- 2.0 继续保持 `cloudSyncEnabled=false`，不阻塞 1.3 的开发和提审。
- 1.3 新增的纯领域规则、视图模型和命令边界必须可被 2.0 复用，避免以后重写。

## 3. 目标用户与关键时刻

### 3.1 核心用户

- 每周在家做饭 2–5 次的个人或两人家庭。
- 会买菜，但经常忘记冰箱库存或重复购买。
- 不追求专业菜谱，想用现有食材快速决定一顿饭。
- 愿意做少量记录，但不能接受复杂表格式维护。

### 3.2 四个高价值时刻

| 时刻 | 用户心里真正的问题 | 产品必须给出的答案 |
|---|---|---|
| 买菜回家 | 这么多东西怎么快速记？ | 最近购入、常用数量、连续录入，尽量少填字段 |
| 打开冰箱前 | 哪些再不吃就晚了？ | 一个明确的“今日优先”行动，而不是一排状态 |
| 准备做饭 | 现在能做什么，为什么推荐它？ | 结合临期覆盖、库存齐全度和耗时给 1 个首选、2 个备选 |
| 吃完以后 | 库存扣对了吗，这次做饭有什么成果？ | 展示扣减结果、处理的临期食材，并能修正误差 |

## 4. 当前体验审计

### 已经做对的部分

- 每次购入形成独立批次，同食材可聚合查看。
- 新鲜度动态计算，措辞不冒充食品安全判断。
- FEFO 跨批次扣减和 CookingRecord 已形成真实闭环。
- 食谱可按库存匹配，缺料可以进入购物清单。
- 已有最近购买、快捷数量、搜索、收藏、空/加载/错误状态。

### 下一版必须修正的断点

| 断点 | 当前表现 | 后果 |
|---|---|---|
| 首页信息多、行动不够单一 | 临期卡和食谱列表并列，用户还要自己组合 | 看完仍不知道今晚做什么 |
| 推荐理由不够直白 | 主要显示匹配度和缺料数 | 缺少“为什么现在应该做它”的驱动力 |
| 日常用量只能通过做菜或整批丢弃变化 | 零食、临时取用、数量估错难修正 | 几天后库存失真，用户不再相信应用 |
| 做菜完成反馈偏技术 | 强调批次扣减和 CookingRecord | 用户感受不到节省、完成或进展 |
| 新用户空冰箱价值出现较晚 | 需要先理解购入表单和食谱状态 | 首次使用容易中断 |
| “解锁”与“能做”并存 | 规则正确，但对新用户可能显得多一步 | 想做菜时被状态概念打断 |

## 5. 北极星指标与护栏

### 5.1 北极星指标

**每周“由库存推荐进入并完成”的做菜次数。**

只有同时满足以下条件才计入：

1. 从首页今日建议或库存食材的推荐入口进入食谱；
2. 完成做菜确认；
3. 产生 CookingRecord 和实际批次扣减。

这个指标同时要求库存有用、推荐可信、用户愿意行动，不能被单纯浏览食谱虚增。

### 5.2 产品目标

| 指标 | 内测验收目标 | 公测观察目标 |
|---|---:|---:|
| 首次录入一项食材的中位时长 | ≤ 45 秒 | ≤ 60 秒 |
| 首页找到“今天先做什么”的中位时长 | ≤ 10 秒 | ≤ 15 秒 |
| 今日建议 → 做菜确认页到达率 | ≥ 60%（任务测试） | ≥ 25% |
| 做菜确认页 → 完成烹饪率 | ≥ 80%（食材齐全任务） | ≥ 55% |
| 盘点 5 项食材的中位时长 | ≤ 30 秒 | ≤ 45 秒 |
| 关键任务成功率 | ≥ 90% | ≥ 85% |
| 严重误扣、丢数据、无法恢复 | 0 | 0 |

公测指标只有在用户知情、隐私文本和数据采集方案完成后才允许上传；在纯本地阶段只保存匿名聚合计数并支持用户主动导出测试报告。

### 5.3 体验护栏

- 不用连续签到、倒计时恐吓或“浪费羞耻”逼迫用户。
- 不把“超过建议期”写成“变质”或“不可食用”。
- 不为了推荐数量牺牲库存准确性。
- 不因 2.0 尚未上线而要求登录。
- 不在首屏增加超过一个主按钮。

## 6. 版本范围

### P0：本版必须完成

#### P0-1 首页“今日行动”

把当前首页的临期卡片和食谱列表重新组织为一个首要决策区：

- 有库存且有可做食谱时，首屏只给一个主建议：`今晚先做「番茄炒蛋」`。
- 明确展示推荐理由，例如：`能用掉 2 种临期食材 · 食材已齐 · 15 分钟`。
- 主按钮：`就做这道`，直接进入做菜确认。
- 次按钮：`换一道`，在最多 3 个候选中轮换，不进入复杂筛选。
- 没有完整可做食谱时，显示最接近的一道：`只差 1 项`，主按钮改为`加入购物清单`。
- 没有库存时，首页用“三步开始”引导首份购入，不展示空白统计。

推荐必须可解释、可测试，不引入黑盒 AI。建议评分：

```text
score =
  临期必选食材覆盖数 × 100
  + 食材完全满足 × 60
  + 已掌握 × 25
  + 30 分钟内完成 × 15
  + 收藏 × 10
  - 缺少必选食材数 × 40
  - 近 3 次重复做过 × 12
```

排序相同则依次按更短时长、食谱 ID 稳定排序。评分与理由生成必须是纯 TypeScript 函数。

#### P0-2 30 秒快速盘点

在“冰箱”页增加明显的`快速盘点`入口，解决非食谱使用和数量估错：

- 按当前最紧急、最近更新最久的食材排序，每次展示一项。
- 快捷动作：`数量没变`、`用掉一些`、`已经用完`、`丢弃`。
- “用掉一些”提供常用步进和精确数量，不要求进入批次详情。
- 聚合食材跨多个批次扣减时复用 FEFO；先预览，确认后提交。
- “丢弃”记录可选原因：忘记吃、买多了、保存不当、其他。
- 结束后显示：`已核对 5 项，冰箱数据已更新`。

库存调整必须新增事实记录，不允许静默覆盖历史；本地版至少记录调整类型、数量、时间和关联批次，为 2.0 的 `InventoryMovement` 保持兼容。

#### P0-3 更快的购入

保留完整表单，同时增加轻量路径：

- 首页点击“购入”先展示最近 6 项，一点即带出上次数量与保存方式。
- 默认购入日期为今天，默认保存方式来自用户偏好。
- 数量提供减号、加号、常用数量；键盘输入仍可用。
- 提交后显示`继续记一项`与`完成`，支持一趟买菜连续录入。
- 从购物清单转购入时必须预填食材、建议数量，并在购入成功后原子勾选原项。
- 所有可选字段默认折叠，主流程只暴露食材、数量、保存方式。

目标是最近购入食材在 3 次有效点击内完成；新食材在 5 次主要操作内完成。

#### P0-4 推荐理由与“一键开做”

所有食谱卡优先回答“为什么适合现在”：

- `可用掉今天优先的番茄`
- `食材已齐`
- `只差鸡蛋`
- `20 分钟内完成`

交互调整：

- `unlockable` 的食谱在用户点击“就做这道”时弹出一次简短说明，可在同一确认动作中完成解锁并进入做菜页。
- `locked` 仍不可做，但直接说明还差什么条件，不只显示“前置未完成”。
- 食材齐全时，从首页到做菜确认不超过 2 次点击。
- 缺 1 项时，从首页加入购物清单不超过 2 次点击。

领域上仍保持 `locked / unlockable / mastered` 三状态；只降低用户理解和操作成本，不篡改原规则。

#### P0-5 做菜完成反馈与安全修正

完成页从“技术提交成功”改为正向结果：

- 标题：`这顿完成了`。
- 展示：做了什么、用了哪些食材、其中几种属于今日优先、冰箱还剩多少种。
- 使用诚实措辞：`优先使用了 2 种临期食材`，不声称“减少了多少浪费”。
- 提供`库存扣减有误`入口，可在 CookingRecord 详情中修正；修正必须产生反向调整记录，不能直接删除历史。
- 完成后推荐一个自然下一步：`查看冰箱`或`把缺的加入下次采购`，不同时放多个主按钮。

#### P0-6 温和的本周成果

“我的”页增加纯本地周报：

- 本周做菜次数。
- 本周优先使用临期食材次数。
- 本周完成/盘点的食材数。
- 本周丢弃记录及原因分布。
- 一条基于事实的建议，例如：`叶菜类连续两周有丢弃记录，下次可以少买一点。`

只有数据充分时才给建议；样本不足时显示“再记录几次，就能看到你的冰箱节奏”。

### P1：P0 稳定后加入

- “常备食材”清单与低库存补货建议。
- 按可用时间筛选：15 分钟、30 分钟、慢慢做。
- 做菜后轻量评价：喜欢 / 一般，以及下次份量建议。
- 首页建议支持“今天不想吃这个”，在本机短期降权。
- 周报可生成不含库存数量和个人标识的分享卡片。

### 本版明确不做

- 不开放登录、家庭共享或真实云同步。
- 不做 AI 对话、拍照识别、条码或小票 OCR。
- 不做营养/医疗结论、热量目标或食品安全判定。
- 不做公开社区、排行榜、签到惩罚或商城。
- 不做需要后台常驻能力的推送提醒；订阅消息放在 2.0 合规完成后评估。

## 7. 页面与交互改造

| 页面 | 本版改动 | 首要验收点 |
|---|---|---|
| 首页 | 今日行动主卡、换一道、推荐理由、空冰箱三步引导 | 10 秒内找到主建议，首屏仅一个主 CTA |
| 冰箱 | 快速盘点入口、最近未核对提示、聚合食材快捷调整 | 5 项盘点 ≤ 30 秒，跨批次使用符合 FEFO |
| 食材详情 | 使用一些、用完、丢弃原因、调整历史 | 任意调整可追溯，可由反向记录修正 |
| 购入食材 | 最近购入首屏、可选项折叠、连续录入 | 最近食材 3 次点击可完成 |
| 食谱库 | 推荐理由、按“现在适合”排序、时间筛选 | 理由与真实库存一致 |
| 食谱详情 | 可解锁食谱合并开做路径、明确锁定条件 | 不改变状态规则，减少一次无意义返回 |
| 做菜确认 | 临期使用提示、扣减预览更聚焦 | 确认前不改库存，失败无半条记录 |
| 做菜完成/记录 | 成果反馈、扣减明细、修正入口 | 修正保留原记录并生成反向事实 |
| 购物清单 | 缺 1 项快速加入、转购入原子完成 | 不生成重复未勾选项 |
| 我的 | 本周成果与基于事实的少买建议 | 无数据时不编造结论 |

## 8. 需要新增或调整的数据

具体字段名可在实现设计时微调，但语义和审计要求不得删除。

### 8.1 库存流水

本地 1.x 增加与 2.0 兼容的库存流水：

```ts
type InventoryMovementType =
  | 'purchase'
  | 'cook_consume'
  | 'manual_consume'
  | 'adjust'
  | 'discard'
  | 'correction';

interface LocalInventoryMovement {
  id: string;
  pantryBatchId: string;
  ingredientId: string;
  type: InventoryMovementType;
  quantityDelta: number;
  unit: string;
  reason?: 'forgot' | 'overbought' | 'storage' | 'other';
  sourceRecordId?: string;
  occurredAt: number;
}
```

约束：

- `quantityDelta` 后的批次数量不得小于 0。
- 做菜、快速使用和丢弃都先生成分配预览，再原子保存批次与流水。
- 修正使用新的 `correction` 流水，不覆盖或删除原事实。
- 数据升级必须从旧快照无损补默认字段，并保留回退备份。

### 8.2 推荐反馈

```ts
interface RecommendationFeedback {
  recipeId: string;
  action: 'shown' | 'opened' | 'dismissed' | 'cooked';
  occurredAt: number;
}
```

只用于本地短期排序和体验验收；默认不上传，不包含搜索原文、设备指纹或个人身份。

### 8.3 周报聚合

周报从 CookingRecord、InventoryMovement 和盘点记录实时计算，不保存不可验证的“节省金额”“减少碳排”或“避免浪费重量”。

## 9. 核心用户流程

### 9.1 首次使用

```text
空首页
  → 选择“记下第一份食材”
  → 选食材 + 数量 + 保存方式
  → 提交
  → 首页立即出现今日建议和可做食谱
```

验收：不阅读帮助文档也能完成；过程中不出现登录、云同步或技术名词。

### 9.2 日常做饭

```text
首页今日行动
  → 就做这道
  → 查看 FEFO 扣减预览
  → 完成烹饪
  → 查看本次成果与更新后的库存
```

验收：食材齐全且食谱已掌握时，从首页开始最多 3 个主要动作完成记录。

### 9.3 库存校准

```text
冰箱 → 快速盘点
  → 数量没变 / 用掉一些 / 用完 / 丢弃
  → 下一项
  → 完成摘要
```

验收：任何失败都停留在当前项并保留已提交项目；不出现负库存和半条流水。

## 10. 开发拆分

建议按 3 个开发迭代和 1 个验证迭代推进；时间是单人全职估算，可根据真实团队调整。

### 迭代 A：行动推荐与数据底座（3–4 天）

- 新增库存流水与旧数据升级。
- 实现可解释推荐评分、推荐理由、重复降权纯函数。
- 实现手动使用/丢弃/修正的 FEFO 分配规则。
- 补领域测试和 Repository 原子性测试。

退出条件：领域规则全部通过，无 UI 也能用测试证明推荐和库存事实正确。

### 迭代 B：首页与低成本维护（4–5 天）

- 首页今日行动卡、换一道、空冰箱引导。
- 冰箱快速盘点和食材详情快捷调整。
- 购入页折叠字段、加减数量、连续录入。
- 补空、加载、错误、提交中与无候选状态。

退出条件：新用户、日常做饭和快速盘点三条原型任务均可走通。

### 迭代 C：做菜驱动力与周报（3–4 天）

- 推荐理由贯穿食谱卡、详情和做菜确认。
- 可解锁食谱的合并开做流程。
- 做菜完成反馈、扣减修正与本周成果。
- 本地匿名体验指标和测试报告导出。

退出条件：完整闭环可证明“建议来自当前库存，完成后库存和成果同步变化”。

### 迭代 D：验收与体验版（3–5 天）

- 自动化、微信开发者工具、真机和异常恢复回归。
- 5–8 位目标用户任务测试，每人完成首次录入、今日决策、做菜和盘点。
- 修复 P0/P1 问题，生成验收报告。
- 上传 `1.3.0` 体验版；不直接提交审核，先收集至少 3 天反馈。

## 11. 自动化验收

除现有全部测试外，至少新增以下测试：

1. 推荐优先覆盖临期必选食材。
2. 食材齐全优先于缺料食谱，但临期覆盖可在定义范围内提升排序。
3. 相同评分稳定排序，结果不随数组顺序漂移。
4. 最近 3 次做过的食谱正确降权，但不会被永久隐藏。
5. 手动“用掉一些”按 FEFO 跨批次扣减。
6. 手动用量超过库存时整体拒绝，库存和流水均不改变。
7. 丢弃生成原因和负向流水，并把用尽批次标为 discarded。
8. CookingRecord 修正生成 correction 流水，原记录仍保留。
9. 快速盘点多项提交中途失败时，已成功项和当前失败项边界清楚。
10. 旧版快照升级后库存、食谱进度、购物清单和做菜记录数量一致。
11. 周报只基于事实计算，不把普通消耗误算为临期处理。
12. 推荐反馈只影响本地排序，不进入业务导出中的敏感字段。
13. 从购物清单转购入时，批次与原购物项在一个 Repository 提交中更新。
14. 首页无库存、无可做食谱、只有 locked 食谱和全部 overdue 都有正确状态。
15. 连续快速点击提交不会生成重复批次或重复 CookingRecord。

发布前必须继续通过：

```bash
pnpm run typecheck
pnpm test
pnpm run check
pnpm run release:check
```

## 12. 人工体验验收脚本

每项必须记录设备、基础库、耗时、结果、截图/录屏和问题编号。

### 场景 A：第一次打开

1. 清空小程序本地数据。
2. 打开首页，不阅读说明。
3. 记录 6 枚鸡蛋。
4. 返回首页并指出今天可以做什么。

通过标准：90 秒内完成；无登录提示；首页立即出现与鸡蛋相关的真实推荐。

### 场景 B：临期驱动做饭

1. 准备两个不同日期的鸡蛋批次和临期番茄。
2. 打开首页，选择首要建议。
3. 完成番茄炒蛋。
4. 检查扣减与成果反馈。

通过标准：推荐理由明确包含临期食材；FEFO 先扣更早到期批次；完成页数字与仓库一致。

### 场景 C：30 秒盘点

1. 准备至少 5 种库存。
2. 分别执行没变、用掉一些、用完和丢弃。
3. 查看盘点摘要和食材详情。

通过标准：中位耗时不超过 30 秒；每项都有可追溯流水；没有负库存。

### 场景 D：缺料到补货

1. 从首页打开只差 1 项的食谱。
2. 加入购物清单。
3. 从购物清单转购入。
4. 返回首页重新查看推荐。

通过标准：无重复购物项；购入字段正确预填；入库后清单被勾选且食谱变为可做。

### 场景 E：误操作恢复

1. 完成一次做菜。
2. 模拟扣减数量填写错误。
3. 使用修正入口恢复数量。

通过标准：原 CookingRecord 保留；产生反向流水；库存恢复正确；再次进入页面结果一致。

### 场景 F：边界与异常

- 存储写入失败。
- 快速重复点击。
- 超长食材名/备注。
- 100 个批次、100 个购物项。
- 当天跨零点重新进入。
- 页面切后台后返回。

通过标准：无白屏、无重复写入、无负库存、无静默数据丢失；错误信息给出下一步。

## 13. 最终验收门禁

只有下表全部满足，才能把版本判定为完成。

| 门禁 | 必须证据 | 通过条件 |
|---|---|---|
| 需求完整 | 本文 P0 逐项对照表 | P0 全部完成；P1 可延期但必须注明 |
| 业务正确 | 自动测试输出 | 现有测试 + 新增测试全绿，0 skipped |
| 数据安全 | Repository/迁移测试 | 页面不直接操作 Storage；升级和失败均不丢数据 |
| 体验效率 | 5–8 人任务测试记录 | 关键任务成功率 ≥ 90%，达到时长目标或有批准的偏差说明 |
| 视觉质量 | 开发者工具与至少 2 台真机截图 | 无遮挡、跳动、文字截断；颜色不是唯一状态表达 |
| 性能 | 真机冷启动与主要页面记录 | 首屏可操作 ≤ 2 秒；主要交互反馈 ≤ 300ms（不含用户网络） |
| 隐私 | 权限与网络检查 | 1.3 不登录、不上传、不新增敏感权限；文案与实际行为一致 |
| 回退 | 备份/恢复演练 | 可恢复到升级前数据；修正不删除历史事实 |
| 发布安全 | `release:check` 与微信问题面板 | 门禁通过，问题面板 0 个业务错误，云同步仍为 false |
| 用户价值 | 内测访谈与指标摘要 | 至少 4/5 用户能说清推荐原因，并愿意在下一次做饭继续使用 |

### 不通过即停止发布的条件

- 任意库存负数、重复扣减、半条 CookingRecord 或不可恢复的数据覆盖。
- 推荐理由与实际库存/新鲜度不一致。
- 用户不理解“超过建议期”并误认为食品安全结论。
- 新用户必须经过登录或 2.0 配置才能使用核心功能。
- 关键路径出现无法解释的 locked/unlockable 阻断。
- 为赶版本而关闭失败测试、跳过数据迁移或直接操作 Storage。

## 14. 最终交付物

版本完成时应提交：

1. `1.3.0` 可导入微信开发者工具的完整工程。
2. P0 功能清单与延期项。
3. 新增/变更数据模型和迁移说明。
4. 自动测试命令、完整结果和新增覆盖列表。
5. 5–8 位用户任务测试记录与结论。
6. 关键页面真机截图或录屏。
7. 性能、异常恢复与回退演练结果。
8. 微信体验版上传记录和版本说明。
9. `TEST_RESULTS.md`、`CHANGELOG.md`、README 与实现状态同步更新。
10. 下一轮建议：基于真实使用证据决定继续优化个人闭环，还是进入 2.0 家庭共享预发。

## 15. 迭代后的决策规则

体验版观察结束后，不凭主观感觉决定下一步：

- 若“今日建议 → 完成做菜”转化达标，但用户频繁出现库存误差，下一版优先继续降低维护成本。
- 若库存准确但推荐打开率低，优先调整推荐理由、候选多样性和首页层级。
- 若个人闭环指标达标，且用户明确提出跨设备/家庭协作需求，再把 2.0 作为下一次用户版本。
- 若多数用户只使用购物清单或购入，不进入做菜闭环，应重新验证产品定位，不能用更多食谱掩盖问题。

最终判断标准不是“功能做完了”，而是用户是否更快知道该吃什么、更愿意真的做一顿饭，并且做完以后仍然相信冰箱里的数。
